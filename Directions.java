// The Directions enum defines the possible movement directions for tiles in the game.
// It includes standard directions: LEFT, RIGHT, UP, DOWN, and an INVALID direction
// to represent an undefined or invalid movement state.

public enum Directions {LEFT, RIGHT, UP, DOWN, INVALID}
